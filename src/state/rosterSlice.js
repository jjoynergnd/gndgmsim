// -----------------------------------------------------------------------------
// File: src/state/rosterSlice.js
// Purpose:
//   Placeholder slice for roster state (players, depth chart, etc.).
// -----------------------------------------------------------------------------

import { createSlice } from "@reduxjs/toolkit";

const rosterSlice = createSlice({
  name: "roster",
  initialState: {},
  reducers: {},
});

export default rosterSlice.reducer;
