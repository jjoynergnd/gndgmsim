  import React from "react";

const StatsPanel = () => {
  return (
    <div
      style={{
        marginTop: "10px",
        border: "1px dashed #c5c5c5",
        borderRadius: "8px",
        padding: "16px",
        background: "#fafafa",
        fontSize: "14px",
      }}
    >
      Season and player stats will live here once the sim engine is in place.
    </div>
  );
};

export default StatsPanel;
