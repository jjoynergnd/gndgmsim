// -----------------------------------------------------------------------------
// File: src/state/seasonSlice.js
// Purpose:
//   Placeholder slice for season progression, schedule, standings, etc.
// -----------------------------------------------------------------------------

import { createSlice } from "@reduxjs/toolkit";

const seasonSlice = createSlice({
  name: "season",
  initialState: {},
  reducers: {},
});

export default seasonSlice.reducer;
