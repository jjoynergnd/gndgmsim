// -----------------------------------------------------------------------------
// File: src/state/draftSlice.js
// Purpose:
//   Placeholder slice for draft board, picks, and scouting state.
// -----------------------------------------------------------------------------

import { createSlice } from "@reduxjs/toolkit";

const draftSlice = createSlice({
  name: "draft",
  initialState: {},
  reducers: {},
});

export default draftSlice.reducer;
